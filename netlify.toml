   [build]
     command = "npm run build"
     publish = "dist"
     functions = "netlify/functions"

[functions.environment]
  VITE_METERED_DOMAIN = "dumasafeguide.metered.live"
  # METERED_SECRET_KEY is set via Netlify dashboard (Site settings > Environment variables) — do not commit an empty value here